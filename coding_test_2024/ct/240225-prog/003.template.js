// URL >

const solution = (n) => {
    let result = 0;
    const arr = n.split("");

    for (let i = 0; i < arr.length; i++) {

    }

    return result;
};

console.log("result:: ", solution("ABCDEF"));
